# Server configuration for Cafe Finder (example)
PORT=3001

# MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=cafefinder

# Auth
JWT_SECRET=change_this_secret

# Uploads
UPLOAD_DIR=uploads

# CORS (optional, leave empty to allow all in dev)
# CORS_ORIGIN=http://localhost:3000