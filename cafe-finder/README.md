# Cafe Finder — знайди каву поруч ☕

Веб-додаток для пошуку кав'ярень поблизу з використанням Google Maps API.

## 🚀 Швидкий старт

### Frontend
1. Відкрийте `index.html` у браузері або використайте локальний сервер
2. Переконайтесь, що Google Maps API ключ налаштований в `index.html`

### Backend
1. Перейдіть в папку `server`:
```bash
cd server
npm install
```

2. Створіть файл `.env` з наступними змінними:
```env
DB_HOST=localhost
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=cafefinder
JWT_SECRET=your-secret-key-change-in-production
PORT=3001
UPLOAD_DIR=uploads
```

3. Створіть базу даних та таблиці:
```bash
mysql -u your_db_user -p your_db_name < schema.sql
```

4. Запустіть сервер:
```bash
node server.js
```

Сервер запуститься на `http://localhost:3001`

## 📋 Функціональність

- 🗺️ Пошук кав'ярень на карті
- 📍 Сортування за відстанню (від найближчих до найвіддаленіших)
- 💖 Додавання в улюблені (свайп вправо)
- 🔍 Фільтри пошуку (радіус, рейтинг, відкрито зараз)
- 📄 Пагінація (показ по 20 закладів)
- 👤 Профіль користувача з аватаром
- ⭐ Відгуки на кав'ярні
- 📝 Списки кав'ярень

## 🔧 Налаштування Google Maps API

Якщо виникає помилка 403 (RefererNotAllowedMapError):

1. Перейдіть до [Google Cloud Console](https://console.cloud.google.com/)
2. Dashboard → APIs & Services → Credentials
3. Знайдіть ваш API ключ та натисніть для редагування
4. В розділі **Application restrictions** виберіть **HTTP referrers (websites)**
5. Додайте:
   ```
   http://localhost/*
   http://127.0.0.1/*
   https://localhost/*
   https://127.0.0.1/*
   ```
6. Переконайтесь, що **Places API** увімкнений

## 📚 API Документація

Детальна документація API доступна в [server/README.md](server/README.md)

### Основні endpoints:
- `POST /api/register` - Реєстрація
- `POST /api/login` - Вхід
- `GET /api/profile` - Профіль користувача
- `POST /api/reviews` - Створити відгук
- `GET /api/reviews` - Отримати відгуки

## 🛠️ Технології

- **Frontend**: Vanilla JavaScript, Google Maps API
- **Backend**: Node.js, Express, MySQL
- **Автентифікація**: JWT
- **Стилі**: CSS3

## 📝 Структура проекту

```
cafe-finder/
├── index.html          # Головна сторінка
├── script.js           # Frontend логіка
├── styles.css          # Стилі
├── server/             # Backend
│   ├── server.js       # Express сервер
│   ├── schema.sql      # Схема бази даних
│   ├── auth.js         # Автентифікація
│   ├── profile.js      # Профіль користувача
│   ├── reviews.js      # Відгуки
│   └── lists.js        # Списки кав'ярень
└── README.md           # Цей файл
```

## 📄 Ліцензія

ISC
